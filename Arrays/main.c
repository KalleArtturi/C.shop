#include <stdio.h>

int main() {

    // arrays in c

    // char[] =array of characters (string)
    // int[] = array of integers
    // float[] = array of floats
    // double[] = array of doubles

    char name[] = "Oh boy how fun this is";
    char grades[] = {'A', 'B', 'C', 'D', 'E', 'F'};

    printf("name: %s\n", name);
    printf("first grade: %c\n", grades[5]);

}